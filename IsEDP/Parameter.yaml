UI:
  Login:
    - user:                                                       # 用例名字的py
      url: '/#/login'                                             # 当前模块url
      className: LoginTest                                        # 类名
      funName:                                                    # key值
      - test_accountErrorsa: {                                    # 用例名称
        Author: [小花, 小王],                                       # 自动化负责人
        Level: P1,                                                # 用例级别
        Scene: '错误的用户名:
                1、输入错误的用户名,
                2、点击确定并断言,
                3、也许错误了吧',                                    # 用例场景
        Elements: {},                                             # 用例中用到的元素
        SendParam: {},                                            # 用例中需要传参的值
        Asserts: 'user {} not exist',                             # 用例断言
        }
        test_number: {                                            # 类下面的下一条用例
        Author: [L, H],                                           # 自动化负责人
        Level: P1,                                                # 用例级别
        Scene: '你猜我猜不猜',                                      # 用例场景
        Elements: {},                                             # 用例中用到的元素
        SendParam: {},                                            # 用例中需要传参的值
        Asserts: 'user {} not exist',                              # 用例断言
        }
